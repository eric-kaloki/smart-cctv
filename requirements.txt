# requirements.txt — Phase 4 Smart CCTV
# Install with: pip install -r requirements.txt

# Core CV pipeline
opencv-python>=4.8.0
numpy>=1.24.0

# AI detection (choose one, both work)
ultralytics>=8.0.0          # YOLOv8/YOLOE-26 — pip install ultralytics
# yolo26-nano                # Lightweight alternative if you have it

# Behavioral memory (local vector DB — no cloud)
chromadb>=0.4.0

# Geometry for zones and tripwires
shapely>=2.0.0

# HTTP for Telegram / webhook
requests>=2.31.0

# Optional: auto-load .env files
python-dotenv>=1.0.0